{
  "name": "@gestao-hb/functions",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "engines": { "node": "22" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "serve": "pnpm build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "@gestao-hb/core": "workspace:*",
    "firebase-admin": "^13.3.0",
    "firebase-functions": "^6.3.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
